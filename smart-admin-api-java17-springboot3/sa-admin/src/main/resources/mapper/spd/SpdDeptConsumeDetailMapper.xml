<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.spd.deptconsume.dao.SpdDeptConsumeDetailDao">

    <select id="selectByConsumeId" resultType="net.lab1024.sa.admin.module.spd.deptconsume.domain.vo.SpdDeptConsumeDetailVO">
        SELECT
            detail_id,
            consume_id,
            material_id,
            material_code,
            material_name,
            specification,
            unit,
            batch_no,
            serial_no,
            expiry_date,
            manufacturer,
            quantity,
            unit_price,
            amount,
            warehouse_id,
            location_id,
            stock_id,
            is_reversed,
            reversed_time,
            reversed_user_name
        FROM spd_dept_consume_detail
        WHERE consume_id = #{consumeId}
        AND del_flag = 0
        ORDER BY create_time
    </select>

</mapper>
